{
  "name": "Macro_Research",
  "install": "sudo apt-get update && sudo apt-get install -y python3.12-venv && python3 -m venv .venv && .venv/bin/python -m pip install --upgrade pip && .venv/bin/pip install -r requirements.txt",
  "terminals": [
    {
      "name": "dashboard",
      "command": ".venv/bin/streamlit run macro_research/dashboard.py --server.port 8501 --server.address 0.0.0.0 --server.headless true"
    }
  ],
  "ports": [8501]
}
